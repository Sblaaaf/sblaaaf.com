# Public URL of the site, used for metadata (Open Graph, canonical URLs).
# Optional on Vercel: falls back to the production domain Vercel provides.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Disable Next.js anonymous telemetry.
NEXT_TELEMETRY_DISABLED=1
